# Это репозиторий для обучения pull request

## Первые шаги

1. Делаем fork репозитория, в которой потом хотим сделать pull request. Ищем кнопку Fork на странице репозитория <https://git@github.com:gulden-geekbrains/version_control.git>
2. Выполняем команду клонирования из своей fork-копии
```sh
git clone git@github.com:*YOURE_GITHUB*/version_control.git
```
3. Создаем новую ветку и вносим необходимые изменения в файл
```sh
git checkout -b updatereadme
vim README.md
git add README.md
git commit -m "Добавили инструкцию как создать pull request"
```
4. Делаем push  
```sh
git push --set-upstream origin updatereadme
```
5. Переходим на свою страницу репозитория. Выбираем ветку **updatereadme** и жмем кнопку **Compare & pull request**

## Заметки

Что бы сделать push от другого пользователя необходимо выполнить команду
```sh
GIT_SSH_COMMAND='ssh -i ~/.ssh/user-private-key -o IdentitiesOnly=yes' git push git@github.com:gulden-geekbrains/version_control.git
```

вместо *user-private-key* подставьте свой ключ

Можно прописать настройки для подсоединения по ssh
```sh
git config remote.origin.url git@github.com:gitusername/reponame
git config core.sshCommand "ssh -i ~/.ssh/user-private-key -o IdentitiesOnly=yes"
```
# Как подружить git с github под Windows 10

Вот видео инструкция https://youtu.be/E8cIjbJMEpE



Git - это консольная утилита, для отслеживания и ведения истории изменения файлов, в вашем проекте. Чаще всего его используют для кода, но можно и для других файлов. Например, для картинок - полезно для дизайнеров.

С помощью Git-a вы можете откатить свой проект до более старой версии, сравнивать, анализировать или сливать свои изменения в репозиторий.

Репозиторием называют хранилище вашего кода и историю его изменений. Git работает локально и все ваши репозитории хранятся в определенных папках на жестком диске.



Cоздание репозитория 
```sh
git init
```

Команда для определения
```sh
git add <имя файла>
```
Сохранение изменений
```sh
git commit -m "по-русски комментарий"
```
История
```sh
git status
```
История коммитов
```sh
git log
```
Изменения
```sh
git diff
```

Удаление файла

```sh

```
Восстановление файлов
```sh
git restore <имя файла>

```
Команда смены директории
```sh
cd .\Version
```
Команда отображения текущей директории
```sh
ls
```
Текущий листинг

```sh
dir
```
Удаление файла
```sh
del <имя файла>
```
Команда инициализирования 
```sh
git init
```

Сохранение  изображения 
```sh
![текст](путь к изображению)
```

Создание новой ветки
```sh
git branch название ветки на анг. Если 2 слова, то между ними _ 
```

Переход на новую ветку 

```sh
git checkout название ветки
```

### Сначала нужно перейти в master 

Слияние веток 

```sh
git merge название новой ветки 
```

Удалить ветку

```agsl
git branch -d навание_ветки
```
# Заголовок первого уровня
## Заголовок второго уровня
### Заголовок третьго уровня

Обычный текст

**Полужирный**

*Курсив*

## Цитирование в языке MarkDown
> Первый уровень
>> Второй уровень
>>> Третий уровень

# Списки 

## Ненумерованый список
* список 
* список
* список

## Нумерованый список
1. один
2. два
3. три

## web ссылки

Тект [ссылка](http.lesson.com   "подсказка")
Добавляем картику в NarkDown

```sh
название ! [комментарий](путь - ссылка или из свойства картинки в папке)
```

Изображение Тибета

![Изображение из компа](388448_O.jpg)
Осень
![Изображение по ссылке](https://gas-kvas.com/uploads/posts/2023-02/1675471666_gas-kvas-com-p-fonovie-risunki-priroda-osen-14.jpg)

Как добавлять новую ветку. Чтобы создать новую ветку в Git необходимо выполнить команду (вместо mybranch укажите название вашей новой ветки):
 git branch mybranch. После выполнения команды, будет создана новая ветка с именем, которое вы указали. Обратите внимание, что вы все еще находитесь в текущей ветке. Переключаемся на новую ветку. Чтобы переключиться на новую ветку, выполните команду: git checkout mybranch.


### Работа с удаленными репозиториями
```sh
git remote -  просмотреть список настроенных удалённых репозиториев
origin — имя по умолчанию, которое Git даёт серверу, с которого производилось клонирование
```
Чтобы просмотреть адреса для чтения и записи -v
```sh
git remote -v -  
origin	https://github.com
origin	https://github.com
```
Добавление удалённых репозиториев
```sh
git remote add <shortname> <url>: git remote
----
origin
git remote add pb https://github.com
git remote -v
origin	https://github.com
```
Получение данных из удалённых записей  
```sh
 git fetch [remote-name]
```
Изменения в удалённых репозиториях
```sh
git push origin master
```
Просмотр удалённого репозитория
```sh
git remote show origin
remote origin
```
Удаление и переименование удалённых репозиториев
```sh
git remote rename pb paul
git remote
```


