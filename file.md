# markdown (инструкция)

## заголовки бывают разные

### нужно поставить решётки в нужном количестве перед текстом (1,2,3,...)

*текст можно писать курсивом, если выделить его звёздочками ( * ) или нижним подчёркиванием ( _ ) с обеих сторон*
**текст можно писать жирным, если выделить его 2 звёздочками ( ** ) или двойными нижними подчёркиваниями ( __ ) с обеих сторон**
разные способы выделения текста нужны, чтобы совмещать эти способы: __вот текст жирный, *но вот тут* он ещё и курсивный__

списки можно сделать:
* ненумерованными (поставив * или + перед текстом)
1. нумерованными (поставив цифру перед текстом)

~зачёркнутый~ (выделить вот таким символом "~")
+ только у меня почему-то не зачеркнулся

-подчёркивание-
+ тоже работает сомнительно, будто не работает

<цитыты>
<<тут тоже должна быть цитата>>

# следующая тема: команды гита и т.д.
1. качаем гит и вс
2. представляемся git config --global user.name \ user.email "тут пишем"
3. создаём папку. открываем её и понеслась

'''sh
вроде тут как консоль должно быть отображено
'''

ctrl+s, git add -> commit -> так всё сохранится
git log –oneline отобразить короче. checkout + хотя бы 4 числа - переместиться к сохранению. checkout master - последнее f5+f9
git diff - показать разницу: красное – удалённое, зелёное - добавленное
git checkout - переход между комитами (надо дописать имя)
+ checkout branch_name - пример
git log -graph визуализировать конфликты

# ветвление

+ почему-то -d не хочет удалять ветки. пролема в слиянии. -D работает

git branch имяНовойВетки - создать какую-то ветвь
git merge имяНовойВетки - залить новую ветку туда, где мы находимся сейчас (перед эти git branch)
git brange -d (или -D) имяНовойВетки - удалить новую ветку (или любую другую)

# картинки

добавляем в папку картинку, add её, потом в игнор (add ignore и вписать имя игнорируемого файла в него)

![хотел найти картинку в поиске яндекса](gitt.png)
+ загрузилась картинка только после слияния веток


# работа с удалённым репозиторием

на github.com находим в поиске https://github.com/gulden-geekbrains/version_control
code и копируем адрес
git clone адрес
dir для листинга директории - папка version_control появилась, но в гите не отобразилась
cd version_control для перехода в эту папку
git status - ветка мейн
git log --oneline --graph посмотреть чо тут было

+ перестал работать git commit -a "". добавил -m и заработало

## создаём свой реп на гх

название без пробелов

### переход в директорию выше локально cd ..

+ ZZ - если перестал раотать q после логов

mkdir one_first создадим там новую папку
cd one_first перейдём в эту папку
echo "# one_first" >> README.md создаём файл ридми
git init локально инициализировать

+ открыть файл через "файл" не получается - не видит. через ctrl+o видит
+ сохранять одновременно файлы из разных репов не получается. один сохраняю в вс, другой в гите

в file переименую ветку как в readme - git branch -M main
в гите делаю реп по умолчанию с хаба git remote add origin https://github.com/ChirkinDmitryPavlovich/one_first.git
git remote show -v подробно посмотреть реп

+ не показывает мне какой там реп. либо я не понял
+ origin вроде значит, что реп тепрь с хаба
+ -m работает у комитов

git push -u origin main - катапульта в сайт

+ file ожидаемо не получилось отправить из другого репа в хаб. readme из нужного репа - всё норм. дружба-логин и т.п.

после "дружения" нужно открыть репозитории на гх, оттуда уже найти загруженные файлы

+ git remote -v показывает где кто куда привязан. фетч и пуш
+ remote show origin более подробная инфа

## редачество из браузера

+ ctrl+s вызывает комитерство в браузере

## забираем изменения в локальную точку pull + теперь push тоже легко делается

+ ветка удаляется локально, но в веб она остаётся - её удалять отдельно в браузере через "всеветки", но можно git push origin --delete secmain

## конфликты

+ реджектед - отказано

при кофликте надо сначала git pull --rebase - всё на комп качаем и сливаем. потом pull --rebase. git rebase --abort если затупы

+ --rebase - без этого конфликт решился, но надо было дописать git rebase --continue и потом уже git push
+ pull почаще, чтоб посвежее было

## форк

добраться до чужого репа
нажать на форк
скопировать себе в реп этот реп на гх

cd ..
mkdir v_c_local
cd v_c_local
code
git clone https://github.com/ChirkinDmitryPavlovich/version_control.git
dir
cd version_control

добавляем уже этот файл в форкнутый реп на гх: физически переносим этот файл в папку, которая init с гх и дальше будем работать в нём через новую ветку

